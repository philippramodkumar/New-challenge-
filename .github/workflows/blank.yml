def upload_poster(file):
    # Simulate the process of uploading a poster
    # In real use, you would save the file to a server or cloud storage here
    # This is a placeholder for demonstration purposes
    if file:  # Assuming file is not None or empty
        print("Poster uploaded successfully!")
        return True
    else:
        print("No file provided.")
        return False

# Example usage:
if __name__ == "__main__":
    fake_file = "example_poster.jpg"  # This would be a file object in a real app
    upload_poster(fake_file)
